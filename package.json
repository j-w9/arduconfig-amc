{
  "name": "arduconfig-amc",
  "private": true,
  "license": "GPL-3.0-only",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "sync": "node scripts/sync-from-vendor.mjs",
    "fixtures": "python3 scripts/gen_expr_fixtures.py",
    "build": "tsc -b tsconfig.json",
    "typecheck": "tsc -b tsconfig.json --pretty false",
    "test": "npm run build && node --test tests/*.test.mjs",
    "sitl:build": "scripts/build-sitl-wasm.sh",
    "sitl:stage": "node scripts/stage-sitl.mjs"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
